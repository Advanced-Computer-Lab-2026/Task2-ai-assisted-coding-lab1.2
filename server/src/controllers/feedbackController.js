import { Feedback } from '../models/Feedback.js';

// TODO: write the validation schemas for create/update per README.md section 2.

// GET /api/feedback
// TODO: implement per README.md section 3.
export async function getAllFeedbacks(req, res, next) {
  try {
    // TODO
  } catch (err) { next(err); }
}

// GET /api/feedback/:id
// TODO: implement per README.md section 3.
export async function getFeedback(req, res, next) {
  try {
    // TODO
  } catch (err) { next(err); }
}

// GET /api/feedback/summary?eventCode=EV101
// TODO: implement per README.md section 4.
export async function getFeedbackSummary(req, res, next) {
  try {
    // TODO
  } catch (err) { next(err); }
}

// POST /api/feedback
// TODO: implement per README.md section 3.
export async function createFeedback(req, res, next) {
  try {
    // TODO
  } catch (err) { next(err); }
}

// PATCH /api/feedback/:id
// TODO: implement per README.md section 3.
export async function updateFeedback(req, res, next) {
  try {
    // TODO
  } catch (err) { next(err); }
}

// DELETE /api/feedback/:id
// TODO: implement per README.md section 3.
export async function deleteFeedback(req, res, next) {
  try {
    // TODO
  } catch (err) { next(err); }
}
